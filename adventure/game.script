function init(self)
	math.randomseed(os.time())
	math.random()
	msg.post("@render:", "clear_color", { color = vmath.vector4(0.099, 0.067, 0.037, 1.0) })
	msg.post("/sounds#music", "play_sound", { gain = 0.1 })
end
